package main

/*
func TestCloneRepo(t *testing.T) {
	link := "https://github.com/matwate/golgc.git"
	repo, Free, err := clone_repo(link)
	if err != nil {
		t.Errorf("Error: %v", err)
	}
	if len(repo) == 0 {
		t.Errorf("Error: %v", "Repo is empty")
	}
	if Free != nil {
		err := Free()
		if err != nil {
			t.Errorf("Error: %v", err)
		}
	}

}
*/
