package main

/*
func TestFileSchemaGen(t *testing.T) {
	var repo_link = "https://github.com/matwate/golgc.git"
	repo_path, _, err := clone_repo(repo_link)
	//defer free()
	err = generate_repo_schema(repo_path)
	if err != nil {
		t.Errorf("Error while generating schema: %v", err)
	}
	if len(global_schemas) == 0 {
		t.Errorf("No schema generated")
	}

	for _, schema := range global_schemas {
		schema.Print()
		println()
	}

}
*/
